CREATE TABLE DC_Patient(
    Patient_ID int NOT NULL PRIMARY KEY,
    First_Name VARCHAR(30) NOT NULL,
    Last_Name VARCHAR(30) NOT NULL,
    Patient_Gender VARCHAR(30) NOT NULL,
    Patient_Phone_Number VARCHAR(15) NOT NULL
);